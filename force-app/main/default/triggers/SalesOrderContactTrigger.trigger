trigger SalesOrderContactTrigger on Order (before insert, before update) {
 /*   if (Trigger.isBefore && (Trigger.isInsert || Trigger.isUpdate)) {
        SalesOrderTriggerHandler.populateContactDetails(Trigger.new);
    }*/
    
    
}