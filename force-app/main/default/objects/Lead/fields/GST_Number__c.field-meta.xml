<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>GST_Number__c</fullName>
    <description>01 → State code (2 digits)

ABCDE → PAN’s first 5 letters

1234 → PAN’s next 4 digits

F → PAN’s last letter

1 → Entity code (1 digit/letter)

Z → Default alphabet &quot;Z&quot;

5 → Checksum (alphanumeric)</description>
    <externalId>false</externalId>
    <label>GST Number</label>
    <length>15</length>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>true</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
