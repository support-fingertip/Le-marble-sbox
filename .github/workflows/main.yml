# ============================================================
# Workflow: Deploy to Salesforce
# Trigger: Manual (workflow_dispatch)
#
# BRANCH: Select from "Use workflow from" dropdown
# Just deploys. No scan, no validation, no tests.
#
# SECRETS: SALESFORCE_AUTH_URL (or SFDX_AUTH_URL_PRODUCTION / _SANDBOX / _DEV)
# ============================================================

name: 02 - Deploy to Salesforce

on:
  workflow_dispatch:
    inputs:
      target_org:
        description: 'Target Salesforce org'
        required: true
        default: 'DEFAULT'
        type: choice
        options:
          - DEFAULT
          - PRODUCTION
          - SANDBOX
          - DEV
      deploy_type:
        description: 'delta = only changed files (last commit) | full = entire source'
        required: true
        default: 'full'
        type: choice
        options:
          - full
          - delta
      metadata_filter:
        description: 'What to deploy'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - apex-only
          - lwc-only
          - objects-only
          - tabs-only
          - flows-only
          - apex-and-lwc
          - tabs-and-objects
          - objects-apex-lwc
          - objects-tabs-apex-lwc
          - apex-lwc-objects-pages
          - custom
      custom_metadata:
        description: 'Only for "custom" — comma-separated paths (e.g. force-app/main/default/classes,force-app/main/default/objects)'
        required: false
        default: ''

jobs:
  deploy:
    name: "Deploy to ${{ github.event.inputs.target_org }} 🚀"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout (${{ github.ref_name }})
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          fetch-depth: 0

      - name: Display Info
        run: |
          echo "╔═════════════════════════════════════════╗"
          echo "║  🌿 Branch      : ${{ github.ref_name }}"
          echo "║  🎯 Target Org  : ${{ github.event.inputs.target_org }}"
          echo "║  📦 Deploy Type : ${{ github.event.inputs.deploy_type }}"
          echo "║  📂 Metadata    : ${{ github.event.inputs.metadata_filter }}"
          echo "╚═════════════════════════════════════════╝"

      - name: Resolve Metadata Paths
        run: |
          FILTER="${{ github.event.inputs.metadata_filter }}"
          case "$FILTER" in
            all)                    echo "SOURCE_PATHS=force-app" >> $GITHUB_ENV ;;
            apex-only)              echo "SOURCE_PATHS=force-app/main/default/classes,force-app/main/default/triggers" >> $GITHUB_ENV ;;
            lwc-only)               echo "SOURCE_PATHS=force-app/main/default/lwc" >> $GITHUB_ENV ;;
            objects-only)           echo "SOURCE_PATHS=force-app/main/default/objects" >> $GITHUB_ENV ;;
            tabs-only)              echo "SOURCE_PATHS=force-app/main/default/tabs" >> $GITHUB_ENV ;;
            flows-only)             echo "SOURCE_PATHS=force-app/main/default/flows" >> $GITHUB_ENV ;;
            apex-and-lwc)           echo "SOURCE_PATHS=force-app/main/default/classes,force-app/main/default/triggers,force-app/main/default/lwc" >> $GITHUB_ENV ;;
            tabs-and-objects)       echo "SOURCE_PATHS=force-app/main/default/tabs,force-app/main/default/objects" >> $GITHUB_ENV ;;
            objects-apex-lwc)       echo "SOURCE_PATHS=force-app/main/default/objects,force-app/main/default/classes,force-app/main/default/triggers,force-app/main/default/lwc" >> $GITHUB_ENV ;;
            objects-tabs-apex-lwc)  echo "SOURCE_PATHS=force-app/main/default/objects,force-app/main/default/tabs,force-app/main/default/classes,force-app/main/default/triggers,force-app/main/default/lwc" >> $GITHUB_ENV ;;
            apex-lwc-objects-pages) echo "SOURCE_PATHS=force-app/main/default/classes,force-app/main/default/triggers,force-app/main/default/lwc,force-app/main/default/objects,force-app/main/default/pages" >> $GITHUB_ENV ;;
            custom)
              CUSTOM="${{ github.event.inputs.custom_metadata }}"
              if [ -z "$CUSTOM" ]; then
                echo "❌ 'custom' selected but no paths provided!"
                exit 1
              fi
              echo "SOURCE_PATHS=$CUSTOM" >> $GITHUB_ENV
              ;;
          esac

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Salesforce CLI
        run: |
          npm install @salesforce/cli --global
          sf --version

      - name: Install sfdx-git-delta
        if: ${{ github.event.inputs.deploy_type == 'delta' }}
        run: echo y | sf plugins install sfdx-git-delta

      - name: Authenticate to Salesforce
        env:
          SALESFORCE_AUTH_URL: ${{ secrets.SALESFORCE_AUTH_URL }}
          SFDX_AUTH_URL_PRODUCTION: ${{ secrets.SFDX_AUTH_URL_PRODUCTION }}
          SFDX_AUTH_URL_SANDBOX: ${{ secrets.SFDX_AUTH_URL_SANDBOX }}
          SFDX_AUTH_URL_DEV: ${{ secrets.SFDX_AUTH_URL_DEV }}
        run: |
          TARGET_ORG="${{ github.event.inputs.target_org }}"
          AUTH_URL=""
          case "$TARGET_ORG" in
            DEFAULT)    AUTH_URL="$SALESFORCE_AUTH_URL" ;;
            PRODUCTION) AUTH_URL="$SFDX_AUTH_URL_PRODUCTION" ;;
            SANDBOX)    AUTH_URL="$SFDX_AUTH_URL_SANDBOX" ;;
            DEV)        AUTH_URL="$SFDX_AUTH_URL_DEV" ;;
          esac
          if [ -z "$AUTH_URL" ]; then AUTH_URL="$SALESFORCE_AUTH_URL"; fi
          if [ -z "$AUTH_URL" ]; then
            echo "❌ No auth secret found! Add SALESFORCE_AUTH_URL in Repo → Settings → Secrets"
            exit 1
          fi
          echo "$AUTH_URL" > ./authfile.txt
          sf org login sfdx-url --sfdx-url-file ./authfile.txt --alias target-org --set-default
          rm -f ./authfile.txt
          echo "✅ Authenticated!"

      # ── DELTA DEPLOY ──
      - name: Generate Delta Package
        if: ${{ github.event.inputs.deploy_type == 'delta' }}
        run: |
          mkdir -p delta-output

          IFS=',' read -ra PATHS <<< "${{ env.SOURCE_PATHS }}"
          SOURCE_ARGS=""
          for P in "${PATHS[@]}"; do
            P=$(echo "$P" | xargs)
            if [ -d "$P" ]; then
              SOURCE_ARGS="$SOURCE_ARGS --source-dir $P"
            fi
          done

          sf sgd source delta \
            --from "HEAD~1" \
            --to "HEAD" \
            --output-dir delta-output \
            --generate-delta \
            $SOURCE_ARGS

          echo "📦 Changed components:"
          echo "───────────────────────────────────────"
          cat delta-output/package/package.xml || echo "(empty)"

          if ! grep -q '<types>' delta-output/package/package.xml 2>/dev/null; then
            echo "⚠️ No changes detected. Nothing to deploy."
            echo "SKIP_DEPLOY=true" >> $GITHUB_ENV
          else
            echo "SKIP_DEPLOY=false" >> $GITHUB_ENV
          fi

      - name: Set skip flag (full)
        if: ${{ github.event.inputs.deploy_type == 'full' }}
        run: echo "SKIP_DEPLOY=false" >> $GITHUB_ENV

      - name: "🚀 Deploy to Salesforce"
        if: ${{ env.SKIP_DEPLOY != 'true' }}
        run: |
          echo "═══════════════════════════════════════"
          echo "  🔥 DEPLOYING TO ${{ github.event.inputs.target_org }}"
          echo "  📂 ${{ github.event.inputs.metadata_filter }}"
          echo "═══════════════════════════════════════"
          echo ""

          if [ "${{ github.event.inputs.deploy_type }}" = "delta" ]; then
            echo "📦 Deploying changed components only..."
            sf project deploy start \
              --manifest delta-output/package/package.xml \
              --target-org target-org \
              --test-level NoTestRun \
              --wait 60
          else
            IFS=',' read -ra PATHS <<< "${{ env.SOURCE_PATHS }}"
            SOURCE_ARGS=""
            for P in "${PATHS[@]}"; do
              P=$(echo "$P" | xargs)
              if [ -d "$P" ]; then
                SOURCE_ARGS="$SOURCE_ARGS --source-dir $P"
              fi
            done

            echo "📂 Deploying: ${{ env.SOURCE_PATHS }}"
            sf project deploy start \
              $SOURCE_ARGS \
              --target-org target-org \
              --test-level NoTestRun \
              --wait 60
          fi

          echo ""
          echo "✅ Deployment SUCCESSFUL!"

      - name: Deploy Destructive Changes
        if: ${{ github.event.inputs.deploy_type == 'delta' && env.SKIP_DEPLOY != 'true' }}
        run: |
          if [ -f "delta-output/destructiveChanges/destructiveChanges.xml" ] && \
             grep -q '<types>' delta-output/destructiveChanges/destructiveChanges.xml 2>/dev/null; then
            echo "🗑️ Deploying deletions..."
            sf project deploy start \
              --manifest delta-output/destructiveChanges/package.xml \
              --post-destructive-changes delta-output/destructiveChanges/destructiveChanges.xml \
              --target-org target-org \
              --test-level NoTestRun \
              --wait 30
          else
            echo "No deletions."
          fi

      - name: Security Cleanup
        if: always()
        run: rm -f authfile.txt authurl.txt

      - name: Summary
        if: always()
        run: |
          echo ""
          echo "╔═════════════════════════════════════════╗"
          echo "║  Branch      : ${{ github.ref_name }}"
          echo "║  Target Org  : ${{ github.event.inputs.target_org }}"
          echo "║  Deploy Type : ${{ github.event.inputs.deploy_type }}"
          echo "║  Metadata    : ${{ github.event.inputs.metadata_filter }}"
          echo "║  Skipped     : ${{ env.SKIP_DEPLOY }}"
          echo "╚═════════════════════════════════════════╝"
